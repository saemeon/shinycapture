% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strategies.R
\name{plotly_strategy}
\alias{plotly_strategy}
\title{Create a Plotly capture strategy}
\usage{
plotly_strategy(
  strip_title = FALSE,
  strip_legend = FALSE,
  strip_annotations = FALSE,
  strip_axis_titles = FALSE,
  strip_colorbar = FALSE,
  strip_margin = FALSE,
  width = NULL,
  height = NULL,
  format = "png"
)
}
\arguments{
\item{strip_title}{Remove the figure title before capture.}

\item{strip_legend}{Hide the legend before capture.}

\item{strip_annotations}{Remove all annotations before capture.}

\item{strip_axis_titles}{Remove x/y axis titles before capture.}

\item{strip_colorbar}{Hide colorbars on all traces before capture.}

\item{strip_margin}{Zero all figure margins before capture, maximizing
the plot area.}

\item{width}{Capture width in pixels. \code{NULL} (default) uses the
element's current rendered width.}

\item{height}{Capture height in pixels. \code{NULL} uses rendered height.
For publication-quality output use e.g. \code{width = 2400, height = 1600}.}

\item{format}{Output image format: \code{"png"} (default), \code{"jpeg"},
\code{"webp"}, or \code{"svg"}.}
}
\value{
A list of class \code{"capture_strategy"} with fields
\code{strategy}, \code{preprocess_js}, and \code{opts}.
Pass to \code{\link{capture_plotly}}.
}
\description{
Generates a \code{capture_strategy} object that uses \code{Plotly.toImage()}
for browser-side capture. Pass it to \code{\link{capture_plotly}}.
}
\details{
Strip flags inject JavaScript that modifies the figure \emph{before}
capture — removing decorations that look good interactively but clutter
a static export (titles, legends, margins). The original figure is
unaffected; modifications happen in a hidden off-screen clone.
}
\examples{
# Default — no stripping, native resolution, PNG
s <- plotly_strategy()

# High-resolution export without title or legend
s <- plotly_strategy(
  strip_title  = TRUE,
  strip_legend = TRUE,
  width  = 2400,
  height = 1600
)

# SVG output
s <- plotly_strategy(format = "svg")
}
\seealso{
\code{\link{capture_plotly}}, \code{\link{html2canvas_strategy}}
}
